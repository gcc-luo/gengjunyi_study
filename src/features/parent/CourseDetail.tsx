import { useMemo, useState } from 'react';
import { Link, useNavigate, useParams } from 'react-router-dom';
import { Modal } from '../../components/Modal';
import { StatusBadge } from '../../components/StatusBadge';
import { useAppStore } from '../../context/AppStore';
import { CourseStatus, type Course } from '../../types/domain';

export function CourseDetail() {
  const { courseId } = useParams(); const navigate = useNavigate(); const { courses, videos, subjects, updateVideo, updateCourse, publishCourse, offlineCourse } = useAppStore();
  const course = courses.find((item) => item.id === courseId); const [error, setError] = useState(''); const [offlineOpen, setOfflineOpen] = useState(false); const [editingVideo, setEditingVideo] = useState<typeof videos[number]>(); const [videoTitle, setVideoTitle] = useState('');
  const orderedVideos = useMemo(() => videos.filter((video) => video.courseId === courseId).sort((a, b) => a.orderIndex - b.orderIndex || a.fileName.localeCompare(b.fileName, 'zh-Hans', { numeric: true })), [videos, courseId]);
  if (!course) return <div className="parent-page"><EmptyDetail title="找不到这门课程" /></div>;
  const subjectName = subjects.find((item) => item.id === course.subjectId)?.name ?? course.subjectId;
  const move = (index: number, direction: -1 | 1) => { const other = orderedVideos[index + direction]; const current = orderedVideos[index]; if (!other || !current) return; updateVideo(current.id, { orderIndex: other.orderIndex }); updateVideo(other.id, { orderIndex: current.orderIndex }); updateCourse(course.id, { videoIds: orderedVideos.map((video, itemIndex) => itemIndex === index ? other.id : itemIndex === index + direction ? current.id : video.id) }); };
  const publish = () => { const result = publishCourse(course.id); setError(result.ok ? '' : result.reason); };
  return <div className="parent-page"><div className="detail-back"><button type="button" onClick={() => navigate('/parent/courses')}>← 返回课程管理</button></div><section className="detail-hero"><span className="detail-cover" style={{ background: `linear-gradient(135deg, ${course.cover.colors.join(',')})` }}>{subjectName.slice(0, 1)}</span><div><p className="eyebrow">{subjectName} · {course.ageRange || '未设置适龄范围'}</p><h1>{course.title}</h1><p>{course.description || '还没有课程简介。'}</p><StatusBadge status={course.status} /></div><div className="detail-actions">{course.status === CourseStatus.PUBLISHED ? <button className="button" type="button" onClick={() => setOfflineOpen(true)}>下架</button> : <button className="button primary" type="button" onClick={publish}>发布</button>}<Link className="button" to={`/parent/uploads?course=${course.id}`}>添加视频</Link></div></section>{error && <p className="form-error" role="alert">{error}</p>}<section className="panel video-panel"><div className="panel-heading"><div><h2>视频目录</h2><p>{orderedVideos.length} 个视频 · 按播放顺序排列</p></div><Link className="text-link" to={`/parent/uploads?course=${course.id}`}>跳转上传入口</Link></div>{orderedVideos.length ? <ol className="video-list" data-testid="video-list">{orderedVideos.map((video, index) => <li key={video.id}><span className="video-number">{index + 1}</span><span className="video-main"><strong>{video.title}</strong><small>{video.fileName} · {Math.floor(video.durationSeconds / 60)} 分钟</small></span><StatusBadge status={video.status} /><div className="video-actions"><button type="button" aria-label={`编辑视频 ${video.title}`} onClick={() => { setEditingVideo(video); setVideoTitle(video.title); }}>编辑</button><button type="button" aria-label={`上移 ${video.title}`} onClick={() => move(index, -1)} disabled={index === 0}>↑ 上移</button><button type="button" aria-label="下移" onClick={() => move(index, 1)} disabled={index === orderedVideos.length - 1}>↓ 下移</button></div></li>)}</ol> : <p className="muted-copy">还没有视频，去上传区添加第一个演示视频。</p>}</section><Modal open={offlineOpen} title="确认下架课程" onClose={() => setOfflineOpen(false)}><p>下架后儿童端将隐藏这门课程，但历史学习记录会保留。</p><div className="form-actions"><button type="button" className="button" onClick={() => setOfflineOpen(false)}>取消</button><button type="button" className="button danger" onClick={() => { offlineCourse(course.id); setOfflineOpen(false); }}>确认下架</button></div></Modal><Modal open={Boolean(editingVideo)} title="编辑视频标题" onClose={() => setEditingVideo(undefined)}><form onSubmit={(event) => { event.preventDefault(); if (!editingVideo || !videoTitle.trim()) { setError('视频标题不能为空'); return; } updateVideo(editingVideo.id, { title: videoTitle.trim() }); setEditingVideo(undefined); setError(''); }}><label>视频标题<input aria-label="视频标题" value={videoTitle} onChange={(event) => setVideoTitle(event.target.value)} /></label><div className="form-actions"><button type="button" className="button" onClick={() => setEditingVideo(undefined)}>取消</button><button type="submit" className="button primary">保存视频</button></div></form></Modal></div>;
}

function EmptyDetail({ title }: { title: string }) { return <div className="panel"><h1>{title}</h1><Link to="/parent/courses">返回课程管理</Link></div>; }
